export class User {
    id: string;
   /** The nom. */
nom:string ;

/** The prenom. */
prenom:string ;

/** The email. */
email:string;

/** The login. */
login:string;

/** The roles. */
roles:string;

 
}